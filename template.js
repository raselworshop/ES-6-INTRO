const firstN = 'jaan';
const lastN = 'pakhi';
const greet = 'potacium'
// const name =  firstN + lastN + greet; //if we want space then procced below
const name = firstN + ' ' + lastN + ' '+ greet;
console.log(name)

const a = 5;
const b = 7;
const result = 'Total sum of '+ a +' '+'and'+' '+b+' '+ 'is '+ (a+b);
console.log(result);

const math = `The sum of ${a} and ${b} total is ${a+b}.`;
console.log(math);


const mail = 'hi john \n' + // to create another new line use \n
            'you are so cute \n'+
            'Have a nice day';
        console.log(mail);